* Renamed the **Baby Van Tilt** setting to **Baby Van Tilt/Wonky Driving** as it has been discovered these issues are caused by the same bug.
* **c7607db**: Added a new **Time Skip** bug fix.
	* **9785693**: Also added a `FixTimeSkip` property to the `[Physics]` section of `BugFixes.ini` to force enable this bug fix in a mod.